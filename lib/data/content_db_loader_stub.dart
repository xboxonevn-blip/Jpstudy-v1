Future<String> ensureContentDbReadyImpl({
  required String assetPath,
  required String fileName,
  required bool forceReplace,
}) {
  throw UnsupportedError('Content DB loading is not supported on this platform.');
}
